# EP_CA_AUG22FT

## Backend installation and setup

## Prerequisites
- Node.js: Make sure that Node.js is installed on your device.

## Clone the repository
1. open the terminal or command prompt.
2. Navigate to the directory where you want to clone the repository.
3. run the following command to clone the repository: 
"git clone https://github.com/fadezilla/Exam-project"

## Install dependencies
1. "cd exam" to navigate to the projects root directory
2. run the following command to install the application dependencies: 
"npm install" 

## Set up the database
1. make sure you have a MySQL server installed and running on your device.
2. Create a new MySQL database for your project.
3. Create a new Schema called "StockSalesDB"
4. Create a admin user for this and give it all privileges like this with this queries:
"create database StockSalesDB;" 
"CREATE USER 'admin'@'localhost' IDENTIFIED WITH mysql_native_password BY 'P@ssw0rd';"
"ALTER USER 'admin'@'localhost' IDENTIFIED WITH mysql_native_password BY 'P@ssw0rd';"
"GRANT ALL PRIVILEGES ON StockScategoriescategoriesalesDB.* TO 'admin'@'localhost';"
5. Update your .env file with your MySQL database details, in this specific project I used the values:
HOST="your host"
USER_ADMIN_PASSWORD="randomPassword"
ADMIN_USERNAME="yourAdminUsername"
ADMIN_PASSWORD="YourAdminPassword"
ADMIN_EMAIL="YourAdminEmail"
DATABASE_NAME="YourDataBaseName"
DIALECT="Mysql"
DIALECTMODEL="Mysql2"
PORT="yourPort"
TOKEN_SECRET="RandomlyGeneratedString"
ITEM_API="http://143.42.108.232:8888/items/stock"

After you have done this you can run the server.

## Start the server
1. in the terminal, navigate to the root directory using "cd exam"
2. run the following command to start the server: "npm start"

## Run the testing

1. first you need to start the server, then open up another terminal within editor.
2. in the new terminal run the command: "npm test" while the server is running. 


## Copy of my .env file:
HOST="localhost"
USER_ADMIN_PASSWORD="P@ssword2023"
ADMIN_USERNAME="Admin"
ADMIN_PASSWORD="Code38951"
ADMIN_EMAIL='admin@admin.com'
DATABASE_NAME="StockSalesDB"
DIALECT="mysql"
DIALECTMODEL="mysql2"
PORT="3000"
TOKEN_SECRET=a471c4599bb6d4ea140cf6cd1bfa5b590ae87f4a70f8e9ce6ab8e9dedbcdf5c6fc8d8f860fa79f4fa6eb9b99adf356abb73e8f7a0ead2d19a27d0427ece978fd
ITEM_API = 'http://143.42.108.232:8888/items/stock'